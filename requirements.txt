# FinanceTracker v1 — backend dependencies
# Pin major versions for reproducibility; all run locally except OpenRouter/Drive calls.

# Web service
fastapi==0.141.1
uvicorn[standard]==0.52.3
python-multipart==0.0.32

# Config
python-dotenv==1.2.3

# HTTP client (OpenRouter)
requests==2.34.2

# Excel
openpyxl==3.1.5

# Google Drive (service account upload)
google-api-python-client==2.198.0
google-auth==2.56.3

# Web Push notifications (VAPID). Only used when PUSH_ENABLED=true with real keys;
# lazy-imported on the enabled send path so an inert install never loads it.
pywebpush==2.4.0

# Tests
pytest==8.3.4
httpx==0.28.1            # required by FastAPI TestClient
